import { StyleSheet } from 'aphrodite';

export const styles = StyleSheet.create({
    formControl: {
        minWidth: 250,
        textAlign: 'left'
    },
    addButton: {
        backgroundColor: '#25f792',
        color: '#000000'
    }
});
